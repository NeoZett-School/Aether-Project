from dataclasses import dataclass;

@dataclass(slots=True)
class User {
    // We still have to fix so that we can define variables witin a class.
    function say_hello() {
        print(f"{self.name} says hello!");
    }
}

user = User(name="Some User");
user.say_hello();